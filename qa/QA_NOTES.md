QA Notes (ZIP6)

This package provides:
- Assembly scripts to merge ZIP4 + ZIP5
- Runtime validation (empty files scan + python compile + docker compose config)

For full end-to-end testing in a connected environment:
- Run backend/unit tests included in ZIP4/ZIP5
- Add playwright/cypress UI tests if required
