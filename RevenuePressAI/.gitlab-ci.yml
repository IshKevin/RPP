stages:
  - qa
  - build

qa:smoke:
  stage: qa
  image: node:20-alpine
  script:
    - node scripts/smoke.mjs

build:
  stage: build
  image: node:20-alpine
  script:
    - npm install
    - npm run db:generate
    - npm run build
  artifacts:
    paths:
      - .next/
